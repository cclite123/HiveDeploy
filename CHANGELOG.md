# 更新日志

本文档记录当前分支相对原项目已经实施的功能增强、兼容性修复和部署调整。

## 2026-08-06 - HiveDeploy 功能增强

本轮代码范围为 `cd484d6` 至 `c5bfca2`，共包含 12 个功能与修复提交。

### 新增公开首页

- 根域名 `/` 不再直接跳转到控制台或只显示登录框，改为完整的 HiveDeploy 产品首页。
- 登录继续使用 `/login`；检测到登录会话时，首页主按钮可直接进入 `/dashboard`。
- 注册按钮会读取站点注册开关、最大用户数和当前用户数，只在允许注册时显示。
- 首页展示 AstrBot + NapCat/LLOneBot 编排、一键连接、持久任务进度、镜像管理和安全清理能力。
- 新增独立首页样式和交互脚本，支持桌面/移动导航、滚动状态、渐入动画和减少动态效果偏好。
- 首页使用当前站点名称，并设置 `no-cache`，避免注册状态或站点信息显示过期。

### 持久化后台任务进度

- 新增 `progress_tasks` 数据表，记录任务 ID、用户、任务类型、目标服务、状态、步骤、详情、错误和时间信息。
- 实例创建、单服务创建、全量更新、单服务更新、Bot 切换、容器重建和端口重建统一写入数据库进度。
- 新增 `GET /api/instance/update_progress/current`，页面刷新或重新打开后可恢复同一个任务的进度框和轮询。
- 成功、失败和最终结果会保留；页面恢复进度不会重复触发创建或更新操作。
- 面板进程重启时，遗留的 `running` 任务会标记为 `interrupted`，不会永久显示为下载中。
- 同一用户已有后台任务运行时，新任务返回 `409`，避免并发拉取、重建或切换互相覆盖。
- 任务按用户隔离，不会读取或覆盖其他用户的进度。

### 镜像源管理

- 新增管理员“镜像管理”页面、`image_sources` 数据表和镜像源管理 API。
- 首次启动自动导入原有 8 个镜像来源，并保留原顺序；Docker Hub 官方源不可删除或停用。
- 管理员可新增、编辑、删除、启停、排序镜像源，并设置全站默认来源。
- 地址统一规范化为 `域名[:端口][/路径前缀]`，拒绝协议、凭据、查询参数、无效端口、非法域名/IP 和非法路径。
- 用户创建实例、创建单服务、更新全部、更新单服务或切换 Bot 时均可选择镜像来源。
- 自动模式按默认源和管理员排序依次尝试；手动指定来源失败后仍会按全站顺序回退。
- 已停用或不存在的来源不能被选择，重复镜像源地址不能保存。

### 网页端安全镜像清理

- 新增 `image_cleanup_runs` 审计表、清理预览、立即执行、最近记录和每日计划设置。
- 定时清理默认关闭，默认时间为 `Asia/Shanghai` 的每天 `03:30`。
- 仅识别 AstrBot、NapCat 和 LLOneBot 三类受管仓库，未知项目镜像不会进入候选列表。
- 每类始终保留最新本地镜像；运行中或已停止容器引用的旧镜像标记为“旧版但正在使用”，不会删除。
- 每次删除前重新读取全部容器引用，出现新引用时跳过，避免预览与删除之间的竞态。
- 删除使用 `force=False` 和 `noprune=True`，不调用 `docker image prune -a`，不删除容器、数据卷或其他项目镜像。
- 手动和定时清理共用单任务锁，避免同时执行；结果记录删除项、跳过项、错误和释放空间。

### AstrBot + NapCat/LLOneBot 一键连接

- `POST /api/instance/auto_config` 会按实例当前 `bot_type` 自动配置 NapCat 或 LLOneBot。
- AstrBot 配置会覆盖为单个 `aiocqhttp` 反向 WebSocket 平台。
- NapCat 配置会覆盖为单个反向 WebSocket 客户端；LLOneBot 配置会覆盖为单个 `ws-reverse` 连接。
- AstrBot 与当前 QQ Bot 使用同一个随机 Token，连接地址使用服务器公开地址和 AstrBot 实际映射的 6199 端口。
- NapCat 与 LLOneBot 都会同时开启 `reportSelfMessage` 和 `debug`。
- 未扫码登录、找不到账号配置或发现多个账号配置时会停止并返回明确错误，不猜测目标 QQ。
- 写配置前先停止 AstrBot 和当前 Bot，再原子写入持久目录并启动容器，避免退出钩子用旧配置覆盖新值。
- 任一持久化写入失败时尝试恢复原配置并重新启动已停止容器。

### LLOneBot 部署与数据持久化

- LLOneBot 与 NapCat 作为互斥 Bot 类型，可在创建、更新和切换流程中选择。
- 新增 `/root/llonebot/data` 持久化挂载，账号配置、WebUI Token、授权数据和一键连接配置可跨重建保留。
- 移除无效的预写 `config/llonebot.json` 方式，只读取并修改扫码登录后生成的真实 `config_<QQ号>.json`。
- WebUI Token 同时兼容 `auth_token.txt` 和 `webui_token.txt`。
- 重建 LLOneBot 时会复用持久化 `AUTH_TOKEN`；仅有一个 `config_<QQ号>.json` 时自动设置 `QUICK_LOGIN_QQ` 恢复账号。
- 已存在 `llonebot_web_proxy_<用户名>` 时不重复占用 WebUI 宿主端口，兼容既有代理部署。
- 账号配置发现不依赖容器内的 `find` 命令，兼容精简版 LLOneBot 镜像。
- 新增 `scripts/migrate_llonebot_data.sh`：自动识别面板实例卷和真正的 LLOneBot 容器，默认仅预览，设置 `DRY_RUN=0` 后执行迁移。

### 日志、终端和文件入口

- 侧边栏、日志切换、终端切换和文件入口只显示 AstrBot 与当前实际部署的 NapCat 或 LLOneBot。
- 未创建实例的账号不显示日志、终端和文件入口。
- 日志页面/API、终端页面和全部文件管理读写 API 都会校验服务是否属于当前实例；手工输入未部署服务地址返回 `404`。
- 文件管理移除“跨服务跳转”区域。
- 修复终端 WebSocket 已连接后，国际化加载把状态文字错误覆盖为“连接中”的问题。
- 终端重连会隔离旧 WebSocket 回调，避免旧连接关闭事件覆盖新连接状态。

### 兼容性与稳定性修复

- 修复镜像管理页面不兼容当前 Starlette `TemplateResponse` 调用签名的问题。
- 修复 LLOneBot 重建后账号、WebUI Token、授权数据或一键连接配置丢失的问题。
- 修复既有 LLOneBot Web 代理占用端口时容器无法重建的问题。
- 修复 LLOneBot 镜像缺少 `find` 时无法一键连接的问题。
- 修复停止容器后写入的连接配置被旧运行时数据覆盖的问题。

### 接口与数据结构

- 新增数据表：`progress_tasks`、`image_sources`、`image_cleanup_runs`。
- 新增页面：`/admin/images`。
- 新增用户接口：`GET /api/instance/update_progress/current`、`GET /api/instance/image_sources`。
- 新增管理员接口：镜像源 CRUD/排序、清理设置、清理预览、立即清理和执行记录。
- 创建、更新、单服务更新和 Bot 切换接口新增可选 `image_source_id`。

### 文档、语言包和测试

- 更新 README、快捷工具提示和全部现有语言包，说明 LLOneBot、镜像源、清理计划和一键连接行为。
- 新增任务进度、镜像管理、一键连接/持久化和服务入口测试，共 32 项单元测试。
- 测试覆盖任务恢复与冲突、多用户隔离、镜像源校验与回退、安全清理竞态、LLOneBot 8.1.5 配置和入口隐藏。

### 当前已部署环境调整

- 已移除旧的 `/usr/local/sbin/hivedeploy-image-cleanup` 及对应 systemd service/timer，避免双重调度。
- 已在面板中启用每天 `03:30` 的内置安全镜像清理；仓库全新安装的默认值仍为关闭。
- 公开首页、动态服务入口、终端状态修复和一键连接增强均已部署。

### 相关提交

| 提交 | 内容 |
| --- | --- |
| `cd484d6` | 持久化后台任务进度 |
| `c72056c` | 镜像源管理与安全清理 |
| `cc1fa4a` | LLOneBot 一键连接与持久化 |
| `bf2f58c` | 兼容已部署 LLOneBot Token 文件 |
| `64c40dd` | 修复镜像管理页面模板兼容性 |
| `80c20e0` | 保留代理部署的 LLOneBot 运行凭据 |
| `4f895ed` | 无 `find` 环境下发现 LLOneBot 账号 |
| `b30167e` | 一键连接配置跨容器重启持久化 |
| `7bd491c` | 重建后自动恢复 LLOneBot 账号 |
| `b98c4e8` | 新增公开产品首页 |
| `20e5b65` | 隐藏并拦截未部署服务工具 |
| `c5bfca2` | 一键连接开启自身消息上报和调试 |

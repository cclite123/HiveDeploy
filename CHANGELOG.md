# 更新日志

本文档记录 HiveDeploy 的重要功能变化、问题修复和升级注意事项。

## 2026-08-06

### 新增

- 新增持久化后台任务进度。创建实例、更新镜像、切换 Bot 和端口重建任务在刷新页面后可继续查看，面板重启遗留任务会标记为已中断。
- 新增镜像源管理。管理员可维护、排序、启停和设置默认来源，用户可在创建、更新和切换 Bot 时选择来源，并在失败后自动回退。
- 新增网页端安全镜像清理。支持预览、立即执行和每日计划，仅处理 AstrBot、NapCat、LLOneBot 的旧镜像。
- 新增 LLOneBot 部署与一键连接支持，包括 WebUI Token、授权文件、账号配置和数据目录持久化。
- 新增公开产品首页，未登录访问根域名时展示产品能力，登录后进入管理面板。

### 改进

- 一键连接会根据实例类型自动配置 NapCat 或 LLOneBot，并与 AstrBot 使用同一随机 Token。
- 一键连接现在同时开启反向 WebSocket 的自身消息上报和调试开关。
- 日志、终端和文件管理入口只显示当前实际部署的服务；直接访问未部署服务会返回 404。
- 文件管理移除跨服务跳转入口。
- 终端连接状态改由 WebSocket 实际状态维护，避免国际化加载后把“已连接”错误覆盖为“连接中”。
- 镜像清理会保留每类最新镜像，并检查运行中及已停止容器；任何仍被容器引用的镜像都不会删除。

### 修复

- 修复 LLOneBot 重建后账号配置、WebUI Token 或授权数据丢失的问题。
- 修复 LLOneBot 容器缺少 `find` 命令时无法发现账号配置的问题。
- 修复停止容器后写入的一键连接配置被退出钩子旧数据覆盖的问题。
- 修复镜像管理页面在当前 Starlette 版本下的模板渲染兼容性问题。

### 升级说明

- 启用网页端自动镜像清理前，应停用并移除旧的 `hivedeploy-image-cleanup.timer`，避免双重调度。
- 清理功能不会调用 `docker image prune -a`，不会删除未知项目镜像、容器或数据卷。
- 现有 LLOneBot 实例如使用旧数据目录，可先运行 `scripts/migrate_llonebot_data.sh` 预览迁移结果，再显式执行迁移。
